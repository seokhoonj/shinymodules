% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/req.R
\name{req_has_rows}
\alias{req_has_rows}
\title{Require a data object with at least one row}
\usage{
req_has_rows(x)
}
\arguments{
\item{x}{A data frame, tibble, data.table, or any object for which \code{nrow(x)} is defined.}
}
\value{
Invisibly returns \code{x} if the requirement is met; otherwise halts the reactive
execution with a silent error (as \code{\link[shiny:req]{shiny::req()}} does).
}
\description{
A convenience wrapper around \code{\link[shiny:req]{shiny::req()}} that checks whether
the given object both exists and contains at least one row.
This is useful inside reactive expressions or render functions
to prevent downstream errors when subsetting empty or NULL data.
}
\examples{
\dontrun{
output$plot <- renderPlot({
  df <- get_data()
  req_has_rows(df)
  plot(df$x, df$y)
})
}

}
\seealso{
\code{\link[shiny:req]{shiny::req()}}
}
